export interface Report {
    id: string;
    name: string;
    scenarioName: string;
    createdAt: string;
    fileUri: string;
}

export interface ReportMetadata {
    id: string;
    name: string;
    scenarioName: string;
    createdAt: string;
}
